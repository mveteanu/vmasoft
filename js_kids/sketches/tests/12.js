
function mouseClicked()
{
    clear();
    text("mouseClicked: " + mouseX + " x " + mouseY, width / 2, height / 2)
}


function mouseMoved()
{
    clear();
    text("mouseMoved: " + mouseX + " x " + mouseY, width / 2, height / 2)
}

function mousePressed()
{
    clear();
    text("mousePressed: " + mouseX + " x " + mouseY, width / 2, height / 2)
}

function mouseReleased()
{
    clear();
    text("mouseReleased: " + mouseX + " x " + mouseY, width / 2, height / 2)
}

function mouseDragged()
{
    clear();
    text("mouseDragged: " + mouseX + " x " + mouseY, width / 2, height / 2)
}

function doubleClicked()
{
    clear();
    text("doubleClicked: " + mouseX + " x " + mouseY, width / 2, height / 2)
}

function mouseWheel()
{
    clear();
    text("mouseWheel: " + mouseX + " x " + mouseY, width / 2, height / 2)
}

function keyPressed()
{
    clear();
    text("KeyPressed: " + mouseX + " x " + mouseY, width / 2, height / 2)
}

function keyReleased()
{
    clear();
    text("keyReleased: " + mouseX + " x " + mouseY, width / 2, height / 2)
}

function keyTyped()
{
    clear();
    text("keyTyped: " + mouseX + " x " + mouseY, width / 2, height / 2)
}
